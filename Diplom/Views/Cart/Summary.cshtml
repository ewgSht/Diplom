@model StoreLib.Entities.Cart

<div id="cart">
    <span class="caption">
        <b>Корзина:</b>
        @Model.Lines.Sum(x => x.Quantity) товаров,
        @Model.ComputeTotalValue().ToString("c")
    </span>
    @Html.ActionLink("Оформить заказ", "Index", "Cart")  
</div>

